// Image assets organized by category
// Use these exports for consistent image imports

// Banners
export const banners = {
  // Banner images are in banners/ subfolder
};

// Categories
export const categories = {
  // Category images are in categories/ subfolder
};

// Lifestyle
export const lifestyle = {
  // Lifestyle images are in lifestyle/ subfolder
};

// Products
export const products = {
  // Product images are in products/ subfolder
};

// Onboarding
export const onboarding = {
  screen1: require('./onboarding/onboarding-screen-1.png'),
  screen2: require('./onboarding/onboarding-screen-2.png'),
  screen3: require('./onboarding/onboarding-screen-3.png'),
};

// Common/Shared images
export const common = {
  // Common images are in common/ subfolder
};

// Note: For specific images, import directly:
// import imageName from '../assets/images/path/to/image.png';

